spring:
  application:
    name: 12_MCPServer_StdIO
  main:
    web-application-type: none
    banner-mode: off

  ai:
    mcp:
      server:
        stdio: true
        annotation-scanner:
          enabled: true   # =default

management:
  endpoints:
    web:
      exposure:
        include: health,metrics