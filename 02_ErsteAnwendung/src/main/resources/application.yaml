spring:
  application:
    name: ErsteAnwendung

  ai:
    ollama:
      chat:
        model: qwen3:8b
      init:
        pull-model-strategy: when_missing
#    openai:
#      api-key: ${MyVerySecretChatGptKey}

  mvc:
    log-request-details: true

logging:
    level:
      org:
        springframework:
          ai:
            chat:
              client:
                advisor: DEBUG