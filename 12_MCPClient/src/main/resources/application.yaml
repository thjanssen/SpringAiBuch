spring:
  application:
    name: 12_MCPClient

  ai:
    openai:
      api-key: ${MyVerySecretChatGptKey}
    mcp:
      client:
        streamable-http:
          connections:
            http1:
              url: http://localhost:7070
#        stdio:
#          root-change-notification: true
#          connections:
#            stdio1:
#              command: java
#              args:
#                - -jar
#                - ./12_MCPServer_StdIO/target/12_MCPServer_StdIO-0.0.1-SNAPSHOT.jar


  mvc:
    log-request-details: true

logging:
    level:
      org:
        springframework:
          ai: DEBUG
          web: DEBUG
      io:
        modelcontextprotocol:
          server: DEBUG

server:
  error:
    include-message: always

management:
  endpoints:
    web:
      exposure:
        include: health,metrics
